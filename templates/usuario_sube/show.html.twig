{% extends 'base.html.twig' %}

{% block title %}UsuarioSube{% endblock %}

{% block body %}
    <h1>UsuarioSube</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario_sube.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usuario_sube_index') }}">back to list</a>

    <a href="{{ path('usuario_sube_edit', {'id': usuario_sube.id}) }}">edit</a>

    {{ include('usuario_sube/_delete_form.html.twig') }}
{% endblock %}
